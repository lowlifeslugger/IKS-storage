# Python Programming — Complete Question Bank
### Goa College of Engineering | Computer Engineering
### Covers: Unit 1 (Computers & OS) · Unit 2 (Python) · Unit 3 (Arrays) · Unit 4 (Functions, Lists, Tuples) · Strings

---

> **Legend:**
> - 🔵 **Theory** — Definition / explanation / comparison question
> - 🟢 **Coding** — Write a program / predict output / trace
> - ⭐ **Important** — Frequently examined

---

# UNIT 1 — Introduction to Computers

## Section A — Short Theory Questions (2 marks)

**Q1.** 🔵 Define a computer. State its two principal characteristics.

**Q2.** 🔵 What is word length? What are the commonly used word lengths in modern computers?

**Q3.** 🔵 Distinguish between a supercomputer and a mainframe computer.

**Q4.** 🔵 Define the term "access time" as it applies to hard disks. What are the three factors that determine it?

**Q5.** 🔵 What is a bitmap? How is it related to scanners?

**Q6.** 🔵 Define OCR. Why is it needed when scanning text?

**Q7.** 🔵 What is a magstripe on a credit card? What is it made of?

**Q8.** 🔵 State two differences between CD-R and CD-RW.

**Q9.** 🔵 What is dot pitch in a monitor? How does it affect image quality?

**Q10.** 🔵 Differentiate between impact and non-impact printers. Give one example of each.

**Q11.** 🔵 What is microcode in the context of a CPU's control unit?

**Q12.** 🔵 What are registers? Where are they located?

**Q13.** 🔵 Differentiate between sequential access and random/direct access storage.

**Q14.** 🔵 What is a head crash in a hard disk? What causes it?

**Q15.** 🔵 State the difference between passive matrix LCD and active matrix LCD (TFT).

---

## Section B — Long Theory Questions (5 marks)

**Q16.** 🔵 ⭐ Describe the seven characteristics of a computer in detail.

**Q17.** 🔵 ⭐ Explain the anatomy of a digital computer. Describe each component with its role.

**Q18.** 🔵 ⭐ Describe the two main parts of the CPU (Control Unit and ALU) with their functions.

**Q19.** 🔵 ⭐ Explain primary memory in detail. Discuss RAM (types), Cache Memory (levels), and ROM.

**Q20.** 🔵 Explain secondary memory. Discuss magnetic tapes, magnetic disks (hard and floppy), and optical disks with their characteristics.

**Q21.** 🔵 Describe all types of keyboard keys with examples for each type.

**Q22.** 🔵 ⭐ Explain the different types of mice. What type is most commonly used and why?

**Q23.** 🔵 Explain how a scanner works. What factors affect scanner quality?

**Q24.** 🔵 Describe the types of scanners. How does a barcode reader work?

**Q25.** 🔵 ⭐ Explain CRT monitors and flat panel monitors (LCD passive, LCD active/TFT, LED). Compare their advantages and disadvantages.

**Q26.** 🔵 Classify monitors based on (a) color and (b) signal type. Give characteristics and examples for each.

**Q27.** 🔵 Describe the seven characteristics of a monitor.

**Q28.** 🔵 ⭐ Explain all types of printers with their working mechanism, advantages, and disadvantages.

---

# UNIT 1 — Operating Systems

## Section A — Short Theory Questions (2 marks)

**Q29.** 🔵 What is the primary purpose of an operating system?

**Q30.** 🔵 What is virtual storage in an OS? How does it increase effective memory?

**Q31.** 🔵 Differentiate between batch processing and online processing in an OS.

**Q32.** 🔵 What is preemptive multitasking? How is it different from cooperative multitasking?

**Q33.** 🔵 What is multithreading? How is it different from multiprocessing?

**Q34.** 🔵 What is an RTOS? Give two real-world applications.

**Q35.** 🔵 Name two mainframe operating systems and their developer.

---

## Section B — Long Theory Questions (5 marks)

**Q36.** 🔵 ⭐ List and explain all six functions of an operating system with examples.

**Q37.** 🔵 ⭐ Classify operating systems based on (a) use and (b) capabilities/features. Explain each type with examples.

**Q38.** 🔵 Compare multi-user, multiprocessing, multitasking, and multithreading operating systems.

---

# UNIT 2 — Introduction to Python

## Section A — Short Theory Questions (2 marks)

**Q39.** 🔵 Who developed Python and in what year? What two languages does it combine features from?

**Q40.** 🔵 What does "dynamically typed" mean in Python?

**Q41.** 🔵 What is the PVM? What is its role in Python execution?

**Q42.** 🔵 What is the difference between `.py` and `.pyc` files?

**Q43.** 🔵 What is byte code in Python? What is its size per instruction?

**Q44.** 🔵 What is a JIT compiler? Why is it sometimes added to PVM?

**Q45.** 🔵 How does Python handle memory allocation differently from C?

**Q46.** 🔵 What is garbage collection in Python? Which module represents it?

**Q47.** 🔵 What are the three generations of objects in Python's garbage collector?

**Q48.** 🔵 What does "batteries included" mean in Python?

**Q49.** 🔵 State two differences between C's `for` loop and Python's `for` loop.

**Q50.** 🔵 What is the difference between "platform independent" and "portable"?

---

## Section B — Long Theory Questions (5 marks)

**Q51.** 🔵 ⭐ List and explain all features of Python.

**Q52.** 🔵 ⭐ Explain the execution of a Python program step by step with a diagram.

**Q53.** 🔵 ⭐ Explain memory management in Python. Discuss the three memory layers.

**Q54.** 🔵 ⭐ Explain garbage collection in Python. What are the three generations? How does reference counting work?

**Q55.** 🔵 ⭐ Compare C and Python on at least 10 points in a tabular format.

---

## Section C — Coding Questions

**Q56.** 🟢 Write a Python program to add two numbers and display their sum.

**Q57.** 🟢 ⭐ Predict the output:
```python
import gc
print(gc.get_threshold())
```

---

# UNIT 2 — Data Types

## Section A — Short Theory Questions (2 marks)

**Q58.** 🔵 What is the difference between a comment and a docstring in Python?

**Q59.** 🔵 Why are triple-quoted strings not recommended as comments in Python?

**Q60.** 🔵 What is NoneType? When does it evaluate to False?

**Q61.** 🔵 State the difference between `bytes` and `bytearray`.

**Q62.** 🔵 What is the difference between `set` and `frozenset`?

**Q63.** 🔵 What is a literal? Give one example of each type of literal in Python.

**Q64.** 🔵 What is the difference between `list` and `tuple`?

**Q65.** 🔵 State the rules for writing identifiers in Python.

**Q66.** 🔵 What is the naming convention for constants in Python? Why doesn't Python enforce it?

**Q67.** 🔵 What does `type()` return? Give two examples.

**Q68.** 🔵 Does Python have a `char` datatype? How are individual characters represented?

**Q69.** 🔵 What is the range of values that can be stored in a `bytes` or `bytearray` object?

---

## Section B — Long Theory Questions (5 marks)

**Q70.** 🔵 ⭐ List and explain all five categories of built-in datatypes in Python with examples and subtypes.

**Q71.** 🔵 ⭐ Explain numeric datatypes in Python: `int`, `float`, `complex`. Include number system representations and type conversions.

**Q72.** 🔵 Explain all six sequence datatypes in Python with examples.

**Q73.** 🔵 Explain `set` and `frozenset` datatypes with their key differences and operations.

**Q74.** 🔵 Explain the `dict` datatype. How are key-value pairs created, accessed, updated, and deleted?

**Q75.** 🔵 Explain naming conventions for packages, modules, classes, functions, variables, and constants in Python.

---

## Section C — Coding Questions

**Q76.** 🟢 ⭐ Write a Python program to demonstrate `int`, `float`, `complex`, `bool`, and `str` types, print each value and its type using `type()`.

**Q77.** 🟢 Write a Python program to:
- Convert decimal 10 to binary, octal, and hexadecimal.
- Convert binary `'1010'`, octal `'12'`, and hex `'A'` to decimal.

**Q78.** 🟢 Predict the output:
```python
a = 10
b = 3.14
c = 3 + 5j
print(type(a), type(b), type(c))
print(int(b), float(a), complex(a))
```

**Q79.** 🟢 Write a Python program to create:
- A `bytes` array with elements `[10, 20, 30, 40]` and display all elements.
- A `bytearray` with elements `[10, 20, 0, 40, 15]`, modify the first two elements to `99` and `88`, then display all elements.

**Q80.** 🟢 Write a Python program to demonstrate set operations — create a set, add elements using `update()`, remove an element, and show that indexing is not allowed.

**Q81.** 🟢 Write a Python program to:
- Create a `frozenset` from a set `{10, 20, 30, 50}`.
- Create a `frozenset` from the string `"abcdefg"`.
- Print both.

**Q82.** 🟢 Write a Python program to create a dictionary with roll numbers as keys and student names as values. Demonstrate: access, update, delete, `keys()`, `values()`.

**Q83.** 🟢 ⭐ Predict the output:
```python
s = {10, 20, 30, 10, 20}
print(s)
print(len(s))

t = (1, 2, 3)
print(t * 2)
print(t + (4, 5))
```

**Q84.** 🟢 Write a Python program to create a list with elements `[10, 20, "abc", "xyz"]` and:
a) Print all elements
b) Print first element
c) Print first two elements
d) Print second and third elements
e) Print last element
f) Print list two times

**Q85.** 🟢 Predict the output:
```python
a = 0b1010
b = 0xFF
c = 0o17
print(a, b, c)
print(type(a))
```

---

# UNIT 2 — Operators

## Section A — Short Theory Questions (2 marks)

**Q86.** 🔵 What is the difference between `/` and `//` operators in Python?

**Q87.** 🔵 Does Python have `++` and `--` operators? What should be used instead?

**Q88.** 🔵 What does the `**` operator do? Give an example.

**Q89.** 🔵 What is the difference between `==` and `is` operators?

**Q90.** 🔵 Explain the `in` and `not in` operators with an example.

**Q91.** 🔵 What is the result of `not 0` and `not 2` in Python? Explain why.

**Q92.** 🔵 What does `~n` evaluate to in Python? Give the formula.

**Q93.** 🔵 State the precedence of `**`, `*`, `+`, and `()` from highest to lowest.

**Q94.** 🔵 What is associativity? Is `**` left-to-right or right-to-left?

**Q95.** 🔵 What is the purpose of the `math` module? Why can't it be used with complex numbers?

---

## Section B — Long Theory Questions (5 marks)

**Q96.** 🔵 ⭐ Explain all nine types of operators in Python with examples.

**Q97.** 🔵 ⭐ Explain all six bitwise operators with their truth tables.

**Q98.** 🔵 Explain operator precedence in Python with a complete table (highest to lowest).

**Q99.** 🔵 Explain the `math` module — how to import it (all 4 methods) and list at least 8 mathematical functions with examples.

---

## Section C — Coding Questions

**Q100.** 🟢 ⭐ Evaluate the following step by step and find the value of `d`:
```python
x, y, z, a, b, c = 1, 2, 3, 2, 2, 3
d = (x + y) * z ** a // b + c
```

**Q101.** 🟢 Predict the output:
```python
x = 20
y = 10
x += y
print(x)
x -= y
print(x)
x *= y
print(x)
x //= y
print(x)
x **= 2
print(x)
```

**Q102.** 🟢 ⭐ Perform the following bitwise operations manually (convert to binary, apply, convert back):
a) `13 & 14`
b) `13 | 14`
c) `13 ^ 14`
d) `10 << 1` and `10 << 2`
e) `10 >> 1` and `10 >> 2`
f) `~2`

**Q103.** 🟢 Predict the output:
```python
a = 5
b = 3
print(a & b)
print(a | b)
print(a ^ b)
print(a << 2)
print(a >> 1)
print(~a)
```

**Q104.** 🟢 Predict the output:
```python
x = 1
y = 2
print(1 and 2)
print(0 and 2)
print(1 or 2)
print(0 or 2)
print(not 2)
print(not 0)
```

**Q105.** 🟢 Predict the output:
```python
a = 25
b = 25
lst1 = [1, 2, 3]
lst2 = [1, 2, 3]

print(a is b)
print(lst1 is lst2)
print(lst1 == lst2)
print(id(a) == id(b))
```

**Q106.** 🟢 Write a Python program to calculate the area of a circle using `math.pi`. Accept radius from the user.

**Q107.** 🟢 Predict the output:
```python
import math
print(math.ceil(4.3))
print(math.floor(4.9))
print(math.sqrt(25))
print(math.factorial(5))
print(math.pow(2, 10))
print(~3)
print(3 ** 2 // 2 + 3)
```

**Q108.** 🟢 Predict the output:
```python
lst = [10, 20, 30, 40, 50]
print(20 in lst)
print(25 not in lst)
for i in lst:
    if i > 30:
        print(i)
```

---

# UNIT 2 — Control Statements

## Section A — Short Theory Questions (2 marks)

**Q109.** 🔵 What is indentation in Python? What is the default number of spaces?

**Q110.** 🔵 What is the purpose of the `pass` statement?

**Q111.** 🔵 What is the difference between `break` and `continue`?

**Q112.** 🔵 When does the `else` suite of a loop execute? When does it NOT execute?

**Q113.** 🔵 What is an infinite loop? Give one example.

**Q114.** 🔵 What is an `AssertionError`? How is it raised?

**Q115.** 🔵 What is the difference between `while` and `for` loops in Python?

**Q116.** 🔵 What is a nested loop? Give a practical example of when it's used.

---

## Section B — Long Theory Questions (5 marks)

**Q117.** 🔵 ⭐ List and explain all control statements available in Python.

**Q118.** 🔵 Explain the `while` loop with syntax, flowchart description, and an example with a trace table.

**Q119.** 🔵 Explain the `for` loop with syntax and examples using: (a) a string, (b) `range()`, (c) a list.

**Q120.** 🔵 Explain `break`, `continue`, and `pass` statements with examples and output.

---

## Section C — Coding Questions

**Q121.** 🟢 ⭐ Write a Python program to display numbers from 1 to 10 using a `while` loop. Show a trace table.

**Q122.** 🟢 Write a Python program to display even numbers from 100 to 200 using a `while` loop.

**Q123.** 🟢 Write a Python program to accept `m` and `n` from the user and display all even numbers from `m` to `n`.

**Q124.** 🟢 ⭐ Predict the output:
```python
x = 10
while x >= 1:
    if x == 5:
        break
    print(x)
    x -= 1
```

**Q125.** 🟢 ⭐ Predict the output:
```python
x = 10
while x >= 1:
    x -= 1
    if x == 5:
        continue
    print(x)
```

**Q126.** 🟢 Write a Python program to display each character of the string `"Python"` using a `for` loop.

**Q127.** 🟢 Write a Python program to display numbers from 10 to 1 in descending order using `range()`.

**Q128.** 🟢 Write a Python program to display elements of a list and find their sum using a `for` loop.

**Q129.** 🟢 ⭐ Write a Python program to display the following pattern:
```
*
* *
* * *
* * * *
* * * * *
```

**Q130.** 🟢 ⭐ Write a Python program to display the following centered triangle (n=5):
```
    *
   * *
  * * *
 * * * *
* * * * *
```

**Q131.** 🟢 Write a Python program to display a 5×5 square of asterisks.

**Q132.** 🟢 Predict the output:
```python
for i in range(5):
    print(i)
else:
    print("Loop done")
```

**Q133.** 🟢 Write a Python program to search for a number in a list using a `for` loop and `else` suite. Display "Found!" or "Not found".

**Q134.** 🟢 Write a Python program to display only negative numbers from a list using the `pass` statement.

**Q135.** 🟢 Write a Python program using `assert` to ensure the user enters a number greater than zero. Handle `AssertionError` using `try-except`.

**Q136.** 🟢 ⭐ Write a Python program to display the prime number series up to 30.

**Q137.** 🟢 ⭐ Write a Python program to generate the Fibonacci series for n terms. Trace the execution for n=6.

**Q138.** 🟢 Predict the output:
```python
for i in range(3):
    for j in range(4):
        print(i, j, end="  ")
    print()
```

**Q139.** 🟢 Write a Python program using a `while` loop to accept numbers from the user until they enter `0`, then display the sum of all entered numbers.

**Q140.** 🟢 Predict the output:
```python
x = 1
while x >= 1:
    print(x, end=" ")
    x += 1
    if x > 5:
        break
```

---

# UNIT 3 — Arrays

## Section A — Short Theory Questions (2 marks)

**Q141.** 🔵 What is an array? How is it different from a list?

**Q142.** 🔵 State three advantages of arrays over lists.

**Q143.** 🔵 What is a type code in Python's `array` module? Give two examples.

**Q144.** 🔵 What is the difference between single-dimensional and multi-dimensional arrays?

**Q145.** 🔵 Why must we use NumPy for multi-dimensional arrays in Python?

**Q146.** 🔵 What are vectorized operations in NumPy? State two advantages.

**Q147.** 🔵 What does `array.tolist()` do?

**Q148.** 🔵 What is the difference between `append()` and `insert()` in arrays?

---

## Section B — Long Theory Questions (5 marks)

**Q149.** 🔵 ⭐ Explain the three ways to import the `array` module with examples.

**Q150.** 🔵 ⭐ Explain indexing and slicing on arrays with examples including negative indices.

**Q151.** 🔵 List and explain all methods available for processing arrays in Python.

**Q152.** 🔵 Explain bubble sort with a step-by-step example. Why is swapping important?

**Q153.** 🔵 Explain sequential search. Compare using a manual loop vs `index()` method.

**Q154.** 🔵 ⭐ Explain NumPy arrays — how to import, create (with different types), and perform vectorized mathematical operations.

---

## Section C — Coding Questions

**Q155.** 🟢 ⭐ Write a Python program using `from array import *` to:
a) Create an integer array `[10, 20, 30, 40, 50]`
b) Display all elements using a `for` loop
c) Display using index access in a `while` loop

**Q156.** 🟢 Write a Python program to create an array named `arr1` with elements `[10, 20, 30, 40]`. Create another array `arr2` by multiplying each element by 5. Print `arr2`.

**Q157.** 🟢 ⭐ Predict the output:
```python
from array import *
x = array('i', [10, 20, 30, 40, 50, 60, 70])
print(x[1:4])
print(x[::2])
print(x[::-1])
print(x[-3:-1])
```

**Q158.** 🟢 Write a Python program to demonstrate array processing methods: `append()`, `insert()`, `remove()`, `pop()`, `index()`, `reverse()`, `count()`.

**Q159.** 🟢 Write a Python program to store students' marks in an array and find total marks and percentage.

**Q160.** 🟢 ⭐ Write a Python program to sort an integer array using **bubble sort**. Show step-by-step swapping. Input: `[5, 3, 8, 1, 9, 2]`.

**Q161.** 🟢 Write a Python program to search for a given element in an array using **sequential search** (without `index()` method). Display its position if found.

**Q162.** 🟢 Write a Python program to search for a given element using the `index()` method. Handle `ValueError` if element is not found.

**Q163.** 🟢 Predict the output:
```python
from array import *
a = array('i', [1, 2, 3, 4, 5])
a.append(6)
a.insert(0, 0)
a.remove(3)
print(list(a))
print(a.count(2))
print(a.index(4))
```

**Q164.** 🟢 Write a Python program using NumPy to:
a) Create an integer array `[1, 2, 3, 4]`
b) Create a float array
c) Create a string array `['Delhi', 'Goa', 'Mumbai']`
d) Print each array and its dtype

**Q165.** 🟢 ⭐ Write a Python program using NumPy to perform the following vectorized operations on `a = [1, 2, 3, 4]` and `b = [10, 20, 30, 40]`:
a) `a + b`
b) `a * b`
c) `a ** 2`
d) `b / a`
e) `np.sqrt(a)`
f) `np.sum(a)`, `np.mean(a)`, `np.max(a)`, `np.min(a)`

**Q166.** 🟢 Predict the output:
```python
import numpy as np
a = np.array([1, 2, 3, 4])
print(a + 10)
print(a * 2)
print(a ** 2)
print(np.sum(a))
print(np.mean(a))
```

---

# UNIT 4 — Functions

## Section A — Short Theory Questions (2 marks)

**Q167.** 🔵 What is the difference between a function and a method in Python?

**Q168.** 🔵 What is a formal argument? What is an actual argument?

**Q169.** 🔵 What is a default argument? Give one example.

**Q170.** 🔵 What is a variable length argument? What symbol is used to define it?

**Q171.** 🔵 What is the difference between `*args` and `**kwargs`?

**Q172.** 🔵 What is a local variable? What is a global variable?

**Q173.** 🔵 When a local and global variable have the same name, which one does the function use?

**Q174.** 🔵 How can a Python function return multiple values? In what form are they returned?

**Q175.** 🔵 What is dynamic typing in the context of functions?

**Q176.** 🔵 State four advantages of using functions.

---

## Section B — Long Theory Questions (5 marks)

**Q177.** 🔵 ⭐ Explain the four types of actual arguments in Python with syntax and examples.

**Q178.** 🔵 ⭐ Explain local and global variables with a program that demonstrates what happens when both have the same name.

**Q179.** 🔵 Explain how a function returns multiple values. How are they received by the caller?

**Q180.** 🔵 Explain `*args` (variable length) and `**kwargs` (keyword variable length) arguments with programs and output.

---

## Section C — Coding Questions

**Q181.** 🟢 ⭐ Write a Python function `add(a, b)` that finds the sum of two numbers and displays the result (without return). Call the function.

**Q182.** 🟢 Write a Python function `add(a, b)` that **returns** the sum. Receive and print the result.

**Q183.** 🟢 ⭐ Write a Python function to check whether a given number is **even or odd**. Return the result.

**Q184.** 🟢 ⭐ Write a Python function to find the **factorial** of a number. Call it in a loop to display factorials from 1 to 10.

**Q185.** 🟢 ⭐ Write a Python function `is_prime(n)` that returns `1` if `n` is prime and `0` if not. Trace the function for `n=5` and `n=4`.

**Q186.** 🟢 Write a Python function that **returns multiple values** — the addition, subtraction, multiplication, and division of two numbers. Receive all four values and print them.

**Q187.** 🟢 ⭐ Write a Python program to demonstrate all four types of actual arguments:
a) Positional arguments
b) Keyword arguments
c) Default arguments
d) Variable length arguments (`*args`)

**Q188.** 🟢 Write a Python program to demonstrate `**kwargs` — accept any number of keyword arguments and display each key-value pair.

**Q189.** 🟢 Predict the output:
```python
x = 100

def func():
    x = 50
    print("Inside:", x)

func()
print("Outside:", x)
```

**Q190.** 🟢 Write a function to accept a list of numbers and return their **total and average**.

**Q191.** 🟢 Write a function to accept a list of strings and display each string.

**Q192.** 🟢 Predict the output:
```python
def greet(name, msg="Hello"):
    print(msg + ", " + name + "!")

greet("Alice")
greet("Bob", "Good morning")
greet(msg="Hi", name="Charlie")
```

**Q193.** 🟢 Predict the output:
```python
def total(*nums):
    s = 0
    for n in nums:
        s += n
    return s

print(total(1, 2, 3))
print(total(10, 20, 30, 40))
print(total(5))
```

---

# UNIT 4 — Lists

## Section A — Short Theory Questions (2 marks)

**Q194.** 🔵 What is a list in Python? How is it different from an array?

**Q195.** 🔵 What does "mutable" mean? Are lists mutable?

**Q196.** 🔵 What is the syntax for slicing a list? What are the default values of start, stop, and step?

**Q197.** 🔵 What does `lst.sort(reverse=True)` do?

**Q198.** 🔵 What is the difference between `remove()` and `pop()` in lists?

**Q199.** 🔵 What does `extend()` do? How is it different from `append()`?

**Q200.** 🔵 How do you create a list using `range()`? Give an example that creates `[5, 7, 9]`.

---

## Section B — Long Theory Questions (5 marks)

**Q201.** 🔵 ⭐ Explain slicing operations on lists with the format `[start:stop:step]`. Give at least 6 examples.

**Q202.** 🔵 ⭐ List and explain all methods available for processing lists in Python with examples.

**Q203.** 🔵 Explain how to update, delete, and reverse elements of a list with programs.

---

## Section C — Coding Questions

**Q204.** 🟢 ⭐ Write a Python program to:
- Create a list with elements from 0 to 9 using `range()`
- Append `10`
- Update 4th element to `40`
- Update 2nd and 3rd elements to `20` and `30`
- Delete first element
- Reverse the list
- Display the list after each operation

**Q205.** 🟢 Write a Python program to access elements of a list using (a) `while` loop and (b) `for` loop.

**Q206.** 🟢 ⭐ Predict the output:
```python
lst = [10, 20, 30, 40, 50]
print(lst[1:3])
print(lst[::2])
print(lst[::-1])
print(lst[-2:])
print(lst[:3])
```

**Q207.** 🟢 Write a Python program to display elements of a list in **reverse order** without using `reverse()`.

**Q208.** 🟢 Write a Python program to concatenate two lists and repeat a list 3 times.

**Q209.** 🟢 ⭐ Write a Python program to find the biggest and smallest elements of a list (a) using `max()` and `min()` and (b) WITHOUT using `max()` and `min()`.

**Q210.** 🟢 ⭐ Write a Python program to sort a list using the `sort()` method (ascending and descending).

**Q211.** 🟢 ⭐ Write a Python program to sort a list **without using the `sort()` method** — use swapping logic.

**Q212.** 🟢 Predict the output:
```python
lst = [3, 1, 4, 1, 5, 9, 2, 6]
lst.sort()
print(lst)
lst.sort(reverse=True)
print(lst)
print(max(lst))
print(min(lst))
print(lst.count(1))
print(lst.index(4))
```

**Q213.** 🟢 Write a Python program to create lists using range in the following ways:
- Numbers from 0 to 4
- Numbers from 2 to 4
- Odd numbers from 5 to 10

---

# UNIT 4 — Tuples

## Section A — Short Theory Questions (2 marks)

**Q214.** 🔵 What is a tuple? How is it different from a list?

**Q215.** 🔵 Why is `(42)` not a tuple but `(42,)` is?

**Q216.** 🔵 Which methods are NOT available for tuples that are available for lists?

**Q217.** 🔵 State one advantage of tuples over lists.

---

## Section B — Long Theory Questions (5 marks)

**Q218.** 🔵 ⭐ Compare lists and tuples in Python on at least 7 points in tabular format.

**Q219.** 🔵 Explain different ways to create tuples with examples. Include: standard, without parentheses, single-element, empty, from a list.

---

## Section C — Coding Questions

**Q220.** 🟢 Write a Python program to create a tuple `(10, 20, "abc", "xyz")` and perform:
a) Print all elements
b) Print first element
c) Print first two elements
d) Print second and third
e) Print last element
f) Print tuple two times
g) Try to modify first element and observe the error

**Q221.** 🟢 ⭐ Predict the output:
```python
tp = (10, 20, 30, 40, 50)
print(tp[1:3])
print(tp[::2])
print(tp[::-1])
print(len(tp))
print(tp + (60, 70))
print(tp * 2)
print(30 in tp)
print(99 not in tp)
```

**Q222.** 🟢 Write a Python program to access all elements of a tuple using (a) `for` loop and (b) `while` loop.

**Q223.** 🟢 Predict the output:
```python
def get_values():
    return 10, 20, 30

a, b, c = get_values()
print(a, b, c)
print(type(get_values()))
```

---

# Strings

## Section A — Short Theory Questions (2 marks)

**Q224.** 🔵 How is a string created in Python? Can single and double quotes be used interchangeably?

**Q225.** 🔵 What is a raw string? What is the effect of adding `r` before a string?

**Q226.** 🔵 What is a Unicode string? How is a Unicode character specified?

**Q227.** 🔵 Why are strings immutable? Give two reasons.

**Q228.** 🔵 What does `rstrip()` do? How is it different from `strip()`?

**Q229.** 🔵 What is the difference between `find()` and `index()`?

**Q230.** 🔵 What does `split(',')` return? Give an example.

**Q231.** 🔵 What does `join()` do? Give the format.

**Q232.** 🔵 What is the difference between `isalpha()` and `isalnum()`?

**Q233.** 🔵 What does `swapcase()` do? Give an example.

**Q234.** 🔵 What is the difference between `startswith()` and `endswith()`?

**Q235.** 🔵 What is `title()` method? Give an example.

---

## Section B — Long Theory Questions (5 marks)

**Q236.** 🔵 ⭐ Explain string indexing and slicing in Python with both positive and negative indices. Give at least 8 slicing examples.

**Q237.** 🔵 Explain why strings are immutable in Python. What happens when we write `s2 = s1`? Explain with `id()`.

**Q238.** 🔵 Explain `find()`, `rfind()`, `index()`, `rindex()` methods with their format, behavior when not found, and an example.

**Q239.** 🔵 Explain `split()` and `join()` methods with examples.

**Q240.** 🔵 ⭐ List and explain all string testing methods (at least 7) with examples.

---

## Section C — Coding Questions

**Q241.** 🟢 ⭐ Predict the output:
```python
s = 'Core Python'
print(s[0])
print(s[-1])
print(s[5])
print(s[0:9:1])
print(s[::2])
print(s[::-1])
print(s[-6::])
print(s[-4:-1])
```

**Q242.** 🟢 Write a Python program on the string `s = 'hello world'`:
a) Display the first character
b) Display `hello`
c) Display `world`
d) Display the last character
e) Display the string in reverse

**Q243.** 🟢 Write a Python program to access each character of a string using:
a) `while` loop with positive index
b) `while` loop with negative index
c) `for` loop
d) `for` loop in reverse using slicing

**Q244.** 🟢 ⭐ Predict the output:
```python
s = 'Core Python'
print(s * 2)
print(s[5:7] * 3)
s1 = 'Core '
s2 = 'Python'
print(s1 + s2)
```

**Q245.** 🟢 Write a Python program to check if a user-entered substring exists in the main string using `in` and `not in`.

**Q246.** 🟢 Write a Python program to compare two strings and determine which comes first in dictionary order.

**Q247.** 🟢 Write a Python program to remove leading, trailing, and both-side spaces from a string. Demonstrate `lstrip()`, `rstrip()`, `strip()`.

**Q248.** 🟢 ⭐ Write a Python program (Program 4-style) to find the **first occurrence** of a substring in a main string using `find()`. Display its 1-based position or "not found".

**Q249.** 🟢 Write the same program using `index()` and handle `ValueError` with `try-except`.

**Q250.** 🟢 ⭐ Write a Python program to find and display **all positions** of a substring in a main string (Program 6/7 style — two versions).

**Q251.** 🟢 Write a Python program using `count()` to:
a) Count occurrences of `'Delhi'` in `'New Delhi is in Delhi'`
b) Count `'e'` from 0 to 2 in the same string
c) Count `'e'` in the full string

**Q252.** 🟢 Predict the output:
```python
s = 'abcd'
print(s[0])
s2 = 'one'
s1 = 'two'
s1 = s2
print(s1)
print(s2)
print(s1 is s2)
```

**Q253.** 🟢 Write a Python program to replace `'girl'` with `'flower'` in the string `'That is a beautiful girl'`. Print both original and new string.

**Q254.** 🟢 Write a Python program to split `'one, two, three, four'` at commas and display each part.

**Q255.** 🟢 Write a Python program to:
a) Join `('one', 'two', 'three')` with hyphen
b) Join `['apple', 'guava', 'grapes', 'mango']` with colon

**Q256.** 🟢 ⭐ Predict the output:
```python
s = 'Python is the future'
print(s.upper())
print(s.lower())
print(s.swapcase())
print(s.title())
```

**Q257.** 🟢 Predict the output:
```python
s = 'This is Python'
print(s.startswith('This'))
print(s.startswith('Hello'))
print(s.endswith('Python'))
print(s.endswith('Java'))
```

**Q258.** 🟢 ⭐ Predict the output:
```python
print('Python3'.isalnum())
print('Python'.isalpha())
print('12345'.isdigit())
print('hello'.islower())
print('HELLO'.isupper())
print('Hello World'.istitle())
print('   '.isspace())
print('Hello3'.isalpha())
```

**Q259.** 🟢 Write a Python program that accepts a string from the user and prints:
- Its length
- First and last character
- String in reverse
- Whether it starts with a capital letter (using `isupper()` on `s[0]`)
- Number of characters using `len()`

**Q260.** 🟢 Write a Python program to accept a group of numbers separated by spaces from the user, split them into a list, and display each number on a new line.

---

# Mixed / Application Questions

**Q261.** 🟢 ⭐ Predict the complete output:
```python
lst = [1, 2, 3, 4, 5]
tp  = (1, 2, 3, 4, 5)
s   = "12345"

print(lst[2])
print(tp[2])
print(s[2])
print(type(lst[2]))
print(type(tp[2]))
print(type(s[2]))
```

**Q262.** 🟢 Predict the output:
```python
def func(a, b=10, *args, **kwargs):
    print(a, b)
    print(args)
    print(kwargs)

func(1, 2, 3, 4, x=5, y=6)
```

**Q263.** 🟢 ⭐ Write a complete Python program that:
- Defines a function `process(nums)` that accepts a list
- Finds and returns: total, average, maximum, minimum, and sorted list
- Calls the function with `[45, 12, 78, 34, 90, 5]` and prints all results

**Q264.** 🟢 Predict the output:
```python
s = "Hello World Python"
words = s.split()
print(len(words))
print(words[0])
print(words[-1])
result = '-'.join(words)
print(result)
```

**Q265.** 🟢 ⭐ Write a Python program to check if a given string is a palindrome (reads the same forward and backward). Examples: `"madam"`, `"racecar"`, `"python"`.

**Q266.** 🟢 Write a Python program to count the number of vowels in a given string.

**Q267.** 🟢 Write a Python program to find the factorial of a number using a `while` loop (without a function).

**Q268.** 🟢 Write a Python program to display all prime numbers between 1 and 100.

**Q269.** 🟢 ⭐ Write a Python program to:
- Create a list of numbers from 1 to 20
- Display only even numbers
- Display only odd numbers
- Find sum of even and sum of odd numbers separately

**Q270.** 🟢 Predict the output:
```python
for i in range(1, 6):
    for j in range(1, i + 1):
        print(j, end=" ")
    print()
```

**Q271.** 🟢 Write a Python program to find the second largest element in a list without using `sort()`.

**Q272.** 🟢 Predict the output:
```python
from array import *
a = array('i', [10, 20, 30, 40, 50])
b = a[1:4]
a[1:3] = array('i', [200, 300])
print(list(a))
print(list(b))
```

**Q273.** 🟢 Write a Python program that uses a dictionary to store names as keys and marks as values. Then:
- Display all student names
- Display all marks
- Find the student with the highest marks

**Q274.** 🟢 ⭐ Predict the output:
```python
def is_prime(n):
    x = 1
    for i in range(2, n):
        if n % i == 0:
            x = 0
            break
    return x

for num in range(2, 20):
    if is_prime(num):
        print(num, end=" ")
```

**Q275.** 🟢 Write a Python program to remove all duplicate elements from a list (use set conversion).

---

# Tricky / Higher-Order Questions

**Q276.** 🟢 ⭐ What is the output? Explain:
```python
a = [1, 2, 3]
b = a
b.append(4)
print(a)
print(b)
print(a is b)
```

**Q277.** 🟢 What is the output? Explain the difference:
```python
a = (1, 2, 3)
b = a
print(a is b)

lst = [1, 2, 3]
tp = tuple(lst)
print(lst == list(tp))
print(type(lst), type(tp))
```

**Q278.** 🔵 ⭐ A student wrote: `t = (42)`. They expected a tuple but got an integer. Explain why, and how to fix it.

**Q279.** 🔵 Explain what happens in memory when the following code runs. Use `id()` to justify:
```python
s1 = 'one'
s2 = 'two'
s2 = s1
```

**Q280.** 🟢 ⭐ Find the output and explain the precedence:
```python
print(2 ** 3 ** 2)       # right-to-left
print((2 ** 3) ** 2)     # left-to-right
print(10 - 3 - 2)        # left-to-right
print(not True or False and True)
```

---

*End of Question Bank — 280 Questions*
